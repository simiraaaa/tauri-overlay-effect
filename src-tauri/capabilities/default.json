{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the overlay window.",
  "windows": ["main", "overlay-monitor-*"],
  "permissions": ["core:default"]
}
